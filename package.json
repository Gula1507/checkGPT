{
  "name": "checkgpt",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "test": "vitest",
    "lint": "eslint .",
    "build": "rm -rf dist && mkdir dist && cp -r src manifest.json dist/"
   },
  "devDependencies": {
    "eslint": "^9.39.2",
    "jsdom": "^27.4.0",
    "vitest": "^1.6.0"
  }
}
