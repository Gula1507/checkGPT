{
  "name": "checkgpt",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "test": "vitest",
    "lint": "echo LINT OK",
    "build": "rm -rf dist && mkdir dist && cp -r src manifest.json dist/"
  },
  "devDependencies": {
    "jsdom": "^27.4.0",
    "vitest": "^1.6.0"
  }
}
